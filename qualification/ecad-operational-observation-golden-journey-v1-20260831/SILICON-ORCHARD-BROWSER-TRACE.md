# SILICON-ORCHARD installed-browser trace

Regeneration note: the accepted Monitor `bb75c4325f903f2c544e9758b5ea8d30c8bbc773` and NQ `78ba5137c83089d6f1cd2bada65f6f7bdda2669c` owner binding re-emitted all 20 condition directories byte-for-byte. The 100 served raw artifacts and compiled UI inputs are unchanged, so this installed-browser trace remains exact fixture evidence; no replacement browser session was required.

Browser: installed Google Chrome `143.0.7499.109`.

Surface: the compiled production Casework UI served by one ephemeral
`127.0.0.1:43191` operator-tool listener. Chrome DevTools used one separate
ephemeral loopback listener solely to read the rendered DOM. All application
assets were loaded from `127.0.0.1:43191`; no external application resource
was admitted.

## ECAD journeys

The direct scheduler/worker contradiction condition route rendered:

- the exact `stage_occurrence` subject and identity digest;
- independently keyed `scheduler_monitor` producer custody;
- exact Monitor and NQ byte/semantic custody;
- the supported-claim deck and unresolved `ecad:worker-observation`
  contradiction;
- independent acquisition, producer, receiver, NQ, Nightshift, and evaluation
  times;
- exact `contradictory` disposition, `claim_contradiction` trigger, and
  `request_qualification_review` next lawful action;
- one presentation-only question.

The direct question route rendered the exact two input/value identities, the
exact next lawful action, `presentation only: true`, and the statement that
the surface records no answer or disposition.

The missing-output raw route rendered all five exact source artifacts:

- Monitor: 2,167 bytes;
- NQ: 8,620 bytes;
- lineage: 9,951 bytes;
- profile: 92 bytes;
- evaluation: 1,346 bytes.

The rendered Monitor/NQ data retained process exit zero, output absence, and
nullable observed-output identity as independent facts. The route exposed each
artifact's SHA-256 digest, byte length, and exact-owner-contract validation
disposition.

## Browser boundary observations

- The skip link was present.
- The only application assets were the local compiled JavaScript and CSS.
- No button, textarea, or editable element was present on condition, question,
  or raw views.
- No aggregate health/result/verdict or target-effect control was rendered.
- The Rust wire tests separately bind bodyless HEAD and exact GET
  status/header/ETag/Content-Length parity for all five raw artifact routes.
